-- This file was protected using Luraph Obfuscator v14.2 [https://lura.ph/]
script.Name = "Skibidi Hub"
if script.Name == "Skibidi Hubt" then
    print("Script đã được đặt tên đúng!")
else
    print("Script đang sử dụng tên sai!")
endlocal allScripts = game.Workspace:GetDescendants()

for _, s in pairs(allScripts) do
    if s:IsA("Script") or s:IsA("LocalScript") then
        if s.Name == "Skibidi Hub" then
            print("Skibidi Hub đã tồnn tại!")
        end
    end
end
-- Tìm người chơi
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

-- Kiểm tra sự tồn tại của nhân vật
if not character then
    warn("Không tìm thấy nhân vật!")
    return
end

-- Xác định các thành phần quan trọng
local humanoid = character:FindFirstChildOfClass("Humanoid")
local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")

if not humanoid or not humanoidRootPart then
    warn("Nhân vật không đầy đủ hoặc bị lỗi!")
    return
end
-- Tạo GUI cơ bản
local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local Button1 = Instance.new("TextButton")
local Button2 = Instance.new("TextButton")

-- Gắn GUI vào người chơi
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Thuộc tính của menu chính
ScreenGui.Name = "MyScriptMenu"
Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.Size = UDim2.new(0, 300, 0, 400)
Frame.Position = UDim2.new(0.5, -150, 0.5, -200)

-- Tiêu đề menu
Title.Parent = Frame
Title.Text = "Blox Fruits Menu"
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 24

-- Nút 1: Kích hoạt farm tự động
Button1.Parent = Frame
Button1.Text = "Auto Farm"
Button1.Size = UDim2.new(0.8, 0, 0, 50)
Button1.Position = UDim2.new(0.1, 0, 0.3, 0)
Button1.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
Button1.TextColor3 = Color3.fromRGB(255, 255, 255)
Button1.Font = Enum.Font.SourceSansBold
Button1.TextSize = 20

-- Nút 2: Kích hoạt Fast Attack
Button2.Parent = Frame
Button2.Text = "Fast Attack"
Button2.Size = UDim2.new(0.8, 0, 0, 50)
Button2.Position = UDim2.new(0.1, 0, 0.5, 0)
Button2.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
Button2.TextColor3 = Color3.fromRGB(255, 255, 255)
Button2.Font = Enum.Font.SourceSansBold
Button2.TextSize = 20
-- In ra thông báo xác nhận
print("Script bắt đầu! Nhân vật của bạn:", player.Name)

-- Các biến toàn cục để dùng cho các chức năng
local enemies = workspace:FindFirstChild("Enemies") -- Đảm bảo thư mục quái tồn tại
if not enemies then
    warn("Không tìm thấy thư mục quái trong workspace!")
end

-- Hàm tiện ích: tìm kiếm quái gần nhất
local function getNearestEnemy()
    local nearestEnemy = nil
    local shortestDistance = math.huge
local CloseButton = Instance.new("TextButton")
CloseButton.Parent = Frame
CloseButton.Text = "X"
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Position = UDim2.new(1, -35, 0, 5)
CloseButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.Font = Enum.Font.SourceSansBold
CloseButton.TextSize = 16

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui.Enabled = false -- Ẩn menu
end)
local Slider = Instance.new("Frame")
Slider.Parent = Frame
Slider.Size = UDim2.new(0.8, 0, 0, 20)
Slider.Position = UDim2.new(0.1, 0, 0.7, 0)
Slider.BackgroundColor3 = Color3.fromRGB(50, 50, 50)

local Handle = Instance.new("Frame")
Handle.Parent = Slider
Handle.Size = UDim2.new(0, 20, 1, 0)
Handle.Position = UDim2.new(0, 0, 0, 0)
Handle.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    for _, enemy in pairs(enemies:GetChildren()) do
        if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
            local distance = (enemy.PrimaryPart.Position - humanoidRootPart.Position).magnitude
            if distance < shortestDistance then
                shortestDistance = distance
                nearestEnemy = enemy
            end
        end
    end

    return nearestEnemy
end

-- Hàm kiểm tra trạng thái của nhân vật
local function checkCharacterStatus()
    if humanoid.Health <= 0 then
        warn("Nhân vật đã chết! Script sẽ tạm dừng.")
        return false
    end
    return true
end

-- Chạy thử một hàm để kiểm tra quái gần nhất
local nearestEnemy = getNearestEnemy()
if nearestEnemy then
    print("Quái gần nhất:", nearestEnemy.Name)
else
    print("Không tìm thấy quái nào gần!")
end
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

local function fastAttack()
    local tool = character:FindFirstChildOfClass("mele") -- Lấy vũ khí hiện tại
    if tool then
        while true do
            tool:Activate() -- Kích hoạt đòn đánh
            wait(0.1) -- Thời gian giữa các lần đánh (giảm xuống để đánh nhanh hơn)
        end
    else
        print("Không tìm thấy vũ khí!")
    end
end

-- Kích hoạt fast attack
fastAttack(1.15)
-- Tìm kiếm quái gần nhất
local function getNearestEnemy()
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local enemies = workspace.Enemies:GetChildren() -- Đảm bảo quái ở đảo "cake island"
    local nearestEnemy = nil
    local shortestDistance = math.huge -- Giá trị lớn vô cực

    for _, enemy in pairs(enemies) do
        if enemy:FindFirstChild("Humanoid") and enemy.Humanoid.Health > 0 then
            local distance = (enemy.PrimaryPart.Position - character.PrimaryPart.Position).magnitude
            if distance < shortestDistance then
                shortestDistance = distance
                nearestEnemy = enemy
            end
        end
    end

    return nearestEnemy
end
local TweenService = game:GetService("TweenService")

local function moveToTarget(target)
    local player = game.Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

    local tween = TweenService:Create(
        humanoidRootPart,
        TweenInfo.new(.02), -- Thời gian di chuyển
        { CFrame = target.PrimaryPart.CFrame }
    )
    tween:Play()
    tween.Completed:Wait()
end
local function attackEnemy(enemy)
    local tool = game.Players.LocalPlayer.Character:FindFirstChildOfClass("Tool")
    if tool then
        tool:Activate() -- Kích hoạt vũ khí
        wait(1) -- Đợi 1 giây giữa các lần tấn công
    end
end
while true do
    local enemy = getNearestEnemy()
    if enemy then
        moveToTarget(enemy)
        while enemy.Humanoid.Health > 0 do
            attackEnemy(enemy)
            wait(0.5) -- Khoảng thời gian giữa các lần tấn công
        end
    else
        print("Không tìm thấy quái. Đợi quái mới xuất hiện...")
        wait(5) -- Đợi 5 giây trước khi tìm kiếm lại
    end
end
